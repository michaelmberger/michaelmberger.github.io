{
  "name": "my-project",
  "source": "src/index.html",
  "type": "module",
  "scripts": {
    "serve": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf ./dist/*",
    "cleanall": "rm -rf ./dist/* && rm -rf ./node_modules/* && npm install"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.12",
    "@tailwindcss/vite":"4.1.12",
    "tailwindcss": "^4.1.12"
  },
  "devDependencies": {
    "vite": "^7.1.2"
  }
}
